server {
    listen 80;
    # server_name: localhost; # Set domain to listen for

    index index.html;

    location / {
	    proxy_set_header Host $host;
	    proxy_set_header X-Real-IP $remote_addr;
	    proxy_pass http://192.168.100.2:8123;
	
    }	
}
